import {downloadFileBase64,downloadBase64FileWithZip,downloadFileBase64List} from './base64'
import {getStore, setStore} from './store'

export {
    downloadFileBase64,
    downloadBase64FileWithZip,
    downloadFileBase64List,
    getStore,
    setStore,
}